name: CI/CD with GitHub Actions | © 2023 by minhchan


on:
  push:
  pull_request:
    branches: [ main ]

jobs: 

  build:

    runs-on: ubuntu-latest


    steps:

      - name: Check-out repository 
        uses: actions/checkout@v3

      - name: Set up JDK 20 
        uses: actions/setup-java@v3 
        with:
          java-version: '20' 
          distribution: 'temurin'
          cache: maven
     
      - name: Build project with Maven
        run: mvn -B package --file Backend/ShoesCustom/ShoesPee/ShoesPee/pom.xml
      
      # - name: Distribute jar-war file 
      #   run: mkdir staging && cp target/*.jar staging     
     
      # - name: Make hyperlink to jar-war file 
      #   uses: actions/upload-artifact@v1
      #   with:
      #     name: 0.0.1-SNAPSHOT.jar  
      #     path: staging
      
#       - name: Build & push Docker image
#         uses: mr-smithers-excellent/docker-build-push@v6
#         with:
#           image: minhchan/minhchan-shoepee
#           tags: latest
#           registry: docker.io
#           dockerfile: Dockerfile
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & push Docker image

      - on:
        push:
          branches:
            - main

      - jobs:
        build:
          runs-on: ubuntu-latest

          steps:
            - name: Checkout repository
              uses: actions/checkout@v2

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v1

            - name: Build & push Docker image
              uses: docker/build-push-action@v2
              with:
                context: .
                push: true
                tags: minhchan/minhchan-shoepee:latest
                platforms: linux/amd64,linux/arm64
                builder: my-builder
                dockerfile: Dockerfile
                username: ${{ secrets.DOCKER_USERNAME }}
                password: ${{ secrets.DOCKER_PASSWORD }}


